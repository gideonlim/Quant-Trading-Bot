# API keys — NEVER commit these
keys.env
.env

# Python
__pycache__/
*.pyc
*.pyo

# Output files (generated at runtime)
# Note: trade_ledger.csv and trade_logs/ are intentionally tracked
# so GitHub Actions can commit them back after each run
universe_cache.json
current_signals.json
backtest_equity.png
backtest_trades.json
backtest_metrics.json
data_cache/

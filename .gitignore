# API keys — NEVER commit these
keys.env
.env

# Python
__pycache__/
*.pyc
*.pyo

# Output files (generated at runtime, local only)
current_signals.json
backtest_equity.png
backtest_trades.json
backtest_metrics.json
data_cache/

# NOTE: The following files are intentionally TRACKED (not ignored)
# so GitHub Actions can persist them across stateless runs:
#   trade_ledger.csv    — Append-only trade log for the financial year
#   trade_logs/         — Per-cycle JSON logs
#   bot_state.json      — Rebalance schedule, peak value, cycle count
#   universe_cache.json — 24h stock universe cache (saves API calls)
